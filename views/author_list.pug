extends layout 

block content
    h1=title

    ul
        each author in author_list
            if author in author_list
            li
                a(href=author.url) #{author.name}
                |    (#{author.author_birth_date_formatted} - #{author.author_death_date_formatted})

        else
            li There are no authors